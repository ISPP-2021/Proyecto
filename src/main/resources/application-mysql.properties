# database init, supports mysql too
database=mysql
spring.datasource.url=jdbc:mysql://localhost/stalion73
spring.datasource.username=stalion73
spring.datasource.password=stalion73
# SQL is written to be idempotent so this is safe
spring.datasource.initialization-mode=always
